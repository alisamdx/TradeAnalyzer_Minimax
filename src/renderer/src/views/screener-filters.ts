// Re-export of shared filter specs so renderer components can import from '@shared/screener-filters'.
// Mirrored here so the renderer can import with the relative path from the web tsconfig.
// The authoritative copy is src/shared/screener-filters.ts.
export { DEFAULT_FILTER_SPECS, type FilterSpec } from '@shared/screener-filters';
