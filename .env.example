# Polygon.io API key. Used in Phase >= 2.
# Prefer storing in OS keychain in production; .env is the dev fallback.
POLYGON_API_KEY=your_polygon_key_here

# Set to 1 to enable live-API smoke tests during `npm test`.
POLYGON_LIVE_TESTS=0
